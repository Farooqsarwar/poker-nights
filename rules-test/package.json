{
  "name": "poker-night-rules-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security-rules tests. Run against the Firestore emulator: npm test",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project poker-night-rules-test \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
